<h1>Hi this is home!</h1>
<p>These are the rides available currently:</p>

<% @trips.each do |t| %>
	<%= "#{t.origin} #{t.destination} #{t.start_time}" %>
	<br>
<% end %>

<p>Also, these are the registered users currently:</p>

<% @users.each do |u| %>
	<%= "#{u.name}" %>
	<br>
<% end %>

<%= link_to "Login", {:controller => 'users', :action => 'login'} %>
<%= link_to "Register", {:controller => 'users', :action => 'new'} %>
